# SkinMiner v1.5 PR-B Validation

Date: 2026-05-10

## 1. Watkinson PDF Batch to Router Stage

Validation input:

```text
outputs/watkinson_v1.5_pr_b_validation/user_pdfs.csv
```

Command:

```bash
python run_pipeline.py \
  --input-pdf-csv outputs/watkinson_v1.5_pr_b_validation/user_pdfs.csv \
  --policy v4_accept_flux \
  --output-dir outputs/watkinson_v1.5_pr_b_validation \
  --stop-after-stage router \
  --no-status-panel
```

Result:

| Check | Result |
| --- | ---: |
| Constructed corpus rows | 3 |
| Rule-pass rows | 3 |
| Access records | 3 |
| Access `status='success'` | 3/3 |
| Access `backend='user_upload'` | 3/3 |
| Router decisions | 3 |
| Router reached | 3/3 |

Router output:

```text
outputs/watkinson_v1.5_pr_b_validation/route_decisions.jsonl
```

Observed router routes:

| DOI | Route | Confidence |
| --- | --- | ---: |
| `10.1159/000183922` | `table` | 0.81 |
| `10.1159/000231528` | `table` | 0.81 |
| `10.1159/000315139` | `table` | 0.81 |

Entry-point validation status: **PASS**.

## 2. PR-A Minimal Regression Check

Input:

```text
outputs/full_run_16_post_all_fixes/patched_area.jsonl
```

Output:

```text
outputs/v1.5_pr_b_validation/pr_a_regression_v4/verified_records.jsonl
```

Result:

| Check | Expected | Observed | Result |
| --- | ---: | ---: | --- |
| Input records | 239 | 239 | PASS |
| v4 verified count | 51 | 51 | PASS |
| `unit_implies_flux` audit tags | 3 | 3 | PASS |

Tagged records:

```text
record_256fed3f4d2f
record_fb26829467a8
record_dea378194778
```

PR-A regression status: **PASS**.

## 3. Static Checks

```bash
python -m py_compile run_pipeline.py schemas/models.py corpus/extract_pdf_metadata.py
python run_pipeline.py --help
```

Both checks passed. `--help` shows `--input-csv` and `--input-pdf-csv` in a mutually exclusive group, plus `--refresh-metadata` and `--stop-after-stage {router,routing}`.

Additional CLI/cache checks:

- Cache hit check returned `extraction_method=cache` for `watkinson_2009_I.pdf`.
- Argparse rejected simultaneous `--input-csv` and `--input-pdf-csv` with the expected mutual-exclusion error.

Overall PR-B validation status: **PASS**.
