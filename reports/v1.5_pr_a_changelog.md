# SkinMiner v1.5 PR-A Changelog

Date: 2026-05-01

Backup directory: `_v1_backup_pr_a_2026-05-01-1841/`

## Modified Code Files

| File | Backup path | Line delta | Change |
| --- | --- | ---: | --- |
| `schemas/models.py` | `_v1_backup_pr_a_2026-05-01-1841/schemas/models.py` | +6 | Added `Record.occlusion` as a `Literal["occluded", "non_occluded", "pretreated", "unknown"]` with default `"unknown"`. |
| `policies/v4_accept_flux.py` | `_v1_backup_pr_a_2026-05-01-1841/policies/v4_accept_flux.py` | +44 | Replaced flux-unit fuzzy matching with a closed `FLUX_UNITS` family, added `_normalize_unit()`, `FLUX_UNITS_NORMALIZED`, `is_flux_unit()`, and v4-only `effective_endpoint_kind()`. |
| `verification/verify_records.py` | `_v1_backup_pr_a_2026-05-01-1841/verification/verify_records.py` | +12 | Added policy-driven effective endpoint kind handling and v4 reclassification audit metadata. |

## New Reports and Outputs

- `reports/v1.5_pr_a_observations.md`
- `reports/v1.5_pr_a_changelog.md`
- `reports/v1.5_pr_a_design_decisions.md`
- `reports/v1.5_pr_a_regression.md`
- `outputs/v1.5_pr_a_regression/v1_rescore_v1.5/verified_records.jsonl`
- `outputs/v1.5_pr_a_regression/v2_rescore_v1.5/verified_records.jsonl`
- `outputs/v1.5_pr_a_regression/v3_rescore_v1.5/verified_records.jsonl`
- `outputs/v1.5_pr_a_regression/v4_rescore_v1.5/verified_records.jsonl`

Temporary scripts were created under `scripts/` for deterministic checks and removed after use.

## Line References

### `schemas/models.py`

- Before: `Record` went from `barrier` directly to `formulation` at backup lines `127-128`.
- After: `Record.occlusion` is defined at current lines `128-133`.

### `policies/v4_accept_flux.py`

- Before: `_looks_like_flux_unit()` used token-based fuzzy matching at backup lines `39-48`; `endpoint_scope_status()` accepted it at backup lines `74-83`.
- After: `FLUX_UNITS` is defined at current lines `9-44`; `_normalize_unit()`/`is_flux_unit()` at current lines `76-86`; v4-only `effective_endpoint_kind()` at current lines `112-115`; `endpoint_scope_status()` uses the effective kind and closed unit family at current lines `117-127`.

### `verification/verify_records.py`

- Before: failure-code logic read `record.endpoint.kind` directly at backup lines `859`, `870`, and `873`.
- After: `_effective_endpoint_kind()` is defined at current lines `46-50`; `_collect_failure_codes()` uses `effective_kind` at current lines `857-885`; `verify_records()` writes reclassification audit keys at current lines `941-944`.

## Verification Summary

- Schema compatibility: `loaded=239 failed=0`; all old rows received `occlusion == "unknown"`.
- Regression counts:
  - v1: `1 -> 1`
  - v2: `25 -> 25`
  - v3: `47 -> 47`
  - v4: `51 -> 51`
- v1-only regressions: `0` for every policy.
- v4 records with `verification_reclassification_reason == "unit_implies_flux"`: `3`.
